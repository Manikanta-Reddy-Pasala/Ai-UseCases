# Architecture — Edge AI Inference Platform

## System Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                     FastAPI Server (:8003)                        │
│                                                                   │
│  POST /api/v1/detect ──────────► Image Upload → Inference        │
│  POST /api/v1/detect/random ───► Synthetic Image → Inference     │
│  GET  /api/v1/model/info ──────► Model Metadata                  │
│  POST /api/v1/model/benchmark ─► N iterations → Stats            │
│  POST /api/v1/model/optimize ──► Optimization Simulation         │
│  GET  / ───────────────────────► Interactive Demo                 │
└────────────────────────────┬─────────────────────────────────────┘
                             │
              ┌──────────────▼──────────────┐
              │    EdgeInferenceEngine       │
              │                             │
              │  ┌────────────────────────┐ │
              │  │     PREPROCESS         │ │
              │  │                        │ │
              │  │  Input Image (H,W,3)   │ │
              │  │         │              │ │
              │  │    Resize (640x640)    │ │
              │  │    via index sampling  │ │
              │  │         │              │ │
              │  │    Normalize /255      │ │
              │  │         │              │ │
              │  │    HWC → NCHW          │ │
              │  │    (1, 3, 640, 640)    │ │
              │  └────────┬───────────────┘ │
              │           │                 │
              │  ┌────────▼───────────────┐ │
              │  │     INFERENCE          │ │
              │  │                        │ │
              │  │  Backend Selection:    │ │
              │  │  ┌──────────────────┐  │ │
              │  │  │ numpy (demo)     │  │ │
              │  │  │ • Mock weights   │  │ │
              │  │  │ • Conv simulate  │  │ │
              │  │  │ • Sigmoid conf   │  │ │
              │  │  ├──────────────────┤  │ │
              │  │  │ openvino (prod)  │  │ │
              │  │  │ • IR model load  │  │ │
              │  │  │ • Static shapes  │  │ │
              │  │  │ • CPU optimized  │  │ │
              │  │  ├──────────────────┤  │ │
              │  │  │ onnx (portable)  │  │ │
              │  │  │ • .onnx model    │  │ │
              │  │  │ • Cross-platform │  │ │
              │  │  └──────────────────┘  │ │
              │  └────────┬───────────────┘ │
              │           │                 │
              │  ┌────────▼───────────────┐ │
              │  │     POSTPROCESS        │ │
              │  │                        │ │
              │  │  Raw Detections        │ │
              │  │  [x1,y1,x2,y2,conf,   │ │
              │  │   class_id]            │ │
              │  │         │              │ │
              │  │  Confidence Filter     │ │
              │  │  (threshold > 0.3)     │ │
              │  │         │              │ │
              │  │  NMS (IoU < 0.5)       │ │
              │  │  Remove overlapping    │ │
              │  │  same-class detections │ │
              │  └────────────────────────┘ │
              └─────────────────────────────┘

              ┌─────────────────────────────┐
              │    ModelOptimizer            │
              │                             │
              │  Input: model_path, config  │
              │                             │
              │  Optimizations:             │
              │  ├── Static Shape (4x)      │
              │  │   Fix input to (1,3,H,W) │
              │  │   No dynamic reshape     │
              │  │                           │
              │  ├── FP16 (1.5x, -50% size) │
              │  │   Float32 → Float16      │
              │  │   Half precision          │
              │  │                           │
              │  └── INT8 (2.5x, -75% size) │
              │      Float32 → Int8         │
              │      Calibration needed     │
              │                             │
              │  Output: estimated speedup, │
              │          size reduction     │
              └─────────────────────────────┘
```

## Detection Classes (RF/Signal Domain)

```
┌─────────────────────────────────────────────┐
│  10 Detection Classes                        │
│                                              │
│  0: frequency_peak     5: carrier_wave       │
│  1: noise_floor        6: modulation_pattern │
│  2: harmonic           7: anomaly            │
│  3: interference       8: background         │
│  4: signal_burst       9: artifact           │
└─────────────────────────────────────────────┘
```

## NMS (Non-Maximum Suppression)

```
Before NMS:                        After NMS:
┌──────────────────────┐          ┌──────────────────────┐
│  ┌─────┐             │          │  ┌─────┐             │
│  │det1 │  ┌────┐     │          │  │det1 │             │
│  │0.92 ├──┤det2│     │  ──────► │  │0.92 │  (det2      │
│  │     │  │0.78│     │  IoU>0.5 │  │     │   removed)  │
│  └─────┘  └────┘     │  remove  │  └─────┘             │
│                      │  lower   │                      │
│       ┌────┐         │  conf    │       ┌────┐         │
│       │det3│         │          │       │det3│ (kept -  │
│       │0.65│         │          │       │0.65│  diff    │
│       └────┘         │          │       └────┘  class)  │
└──────────────────────┘          └──────────────────────┘
```

## Technology Stack

| Component | Technology | Purpose |
|-----------|-----------|---------|
| Inference | NumPy (demo) / OpenVINO / ONNX | Model execution |
| Image | Pillow, NumPy | Image preprocessing |
| Metrics | psutil | Memory benchmarking |
| API | FastAPI | REST endpoints |
| Optimization | Custom simulator | Speedup estimation |
